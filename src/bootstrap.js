import $ from 'jquery';
import Popper from 'popper.js';
import 'bootstrap';

// Torna jQuery e Popper globais (obrigatório para Bootstrap 4 funcionar)
window.$ = $;
window.jQuery = $;
window.Popper = Popper;